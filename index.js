
function greet(name, lastname, age){
    document.write(`Hello ${name} ${lastname}. I am ${age} years old.` );
}

greet('Ankit','Sharma',26);

// calculating square
function square (number) {
    return number*number;
}
document.write('<br>'+square(3));